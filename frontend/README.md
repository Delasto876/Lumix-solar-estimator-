# Electrical Simulator Frontend (Phase 1)

## Run locally
```bash
cd frontend
npm install
npm run dev
```

The app runs with a locked layout (TopBar / Canvas / Details Panel / Bottom Status) and supports basic SVG wiring, selection, and JSON export.
